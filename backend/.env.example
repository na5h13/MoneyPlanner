# ============================================================
# MoneyPlanner Backend Environment Variables
# ============================================================
# Copy this file to .env:
#   cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# SERVER
# ------------------------------------------------------------
PORT=3000
NODE_ENV=development

# ------------------------------------------------------------
# FIREBASE ADMIN SDK
# ------------------------------------------------------------
# Option A: Path to your serviceAccountKey.json file
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./serviceAccountKey.json

# Option B: Base64-encoded service account JSON (better for Railway)
# Generate with: cat serviceAccountKey.json | base64
# FIREBASE_SERVICE_ACCOUNT_KEY_BASE64=

# Firebase project ID (same as client-side)
FIREBASE_PROJECT_ID=

# ------------------------------------------------------------
# PLAID (Server-side secrets)
# ------------------------------------------------------------
# Plaid Dashboard → Keys
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Plaid webhook URL (your Railway backend URL + webhook path)
# e.g., https://your-app.up.railway.app/api/plaid/webhook
PLAID_WEBHOOK_URL=

# ------------------------------------------------------------
# ENCRYPTION
# ------------------------------------------------------------
# Used to encrypt Plaid access tokens stored in Firestore
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# ------------------------------------------------------------
# CORS
# ------------------------------------------------------------
# Allowed origins (comma-separated)
# In production, set to your actual domain
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006

# ------------------------------------------------------------
# RAILWAY (auto-set by Railway, but define for local dev)
# ------------------------------------------------------------
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PUBLIC_DOMAIN=your-app.up.railway.app
