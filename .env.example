# ============================================================
# MoneyPlanner — Frontend Environment Variables
# ============================================================
# Copy to .env and fill in your values:
#   cp .env.example .env
# NEVER commit .env to git (it's in .gitignore).
# ============================================================

# --- Firebase (Client-side) ---
# Firebase Console → Project Settings → General → Web app config
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=

# --- Google OAuth Client IDs ---
# Google Cloud Console → APIs & Services → Credentials
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=

# --- Plaid (Client-side) ---
EXPO_PUBLIC_PLAID_ENV=sandbox
EXPO_PUBLIC_PLAID_ANDROID_PACKAGE_NAME=com.na5h13.keel
EXPO_PUBLIC_PLAID_IOS_BUNDLE_ID=com.na5h13.keel

# --- Backend API URL ---
# Local dev: http://localhost:5050
# Railway: https://your-app.up.railway.app
EXPO_PUBLIC_API_BASE_URL=http://localhost:5050

# --- Expo ---
EXPO_PUBLIC_PROJECT_ID=

# ============================================================
# BACKEND SECRETS → backend/.env (NOT in app bundle)
# ============================================================
# See backend/.env.example for server-side variables.
# Backend secrets go in Railway Variables, not here.
