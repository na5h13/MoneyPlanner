# ============================================================
# MoneyPlanner Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git. It's already in .gitignore.
# ============================================================

# ------------------------------------------------------------
# FIREBASE (Client-side — used in the Expo app)
# ------------------------------------------------------------
# Find these in Firebase Console → Project Settings → General → Your apps → Web app
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
FIREBASE_MEASUREMENT_ID=

# ------------------------------------------------------------
# GOOGLE AUTH (OAuth 2.0)
# ------------------------------------------------------------
# Google Cloud Console → APIs & Services → Credentials
# You need separate client IDs for each platform
# Android: Create "Android" OAuth client → use your app's SHA-1 fingerprint
# iOS: Create "iOS" OAuth client → use your app's bundle ID
# Web: Create "Web" OAuth client (for Expo web + OAuth redirects)
GOOGLE_WEB_CLIENT_ID=
GOOGLE_ANDROID_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=

# ------------------------------------------------------------
# PLAID (Client-side — only public key/env needed in app)
# ------------------------------------------------------------
# Plaid Dashboard → Keys
# PLAID_ENV: sandbox | development | production
PLAID_ENV=sandbox
PLAID_ANDROID_PACKAGE_NAME=com.moneyplanner.app
PLAID_IOS_BUNDLE_ID=com.moneyplanner.app

# ------------------------------------------------------------
# BACKEND / RAILWAY (API URL the app calls)
# ------------------------------------------------------------
# Your Railway deployment URL (no trailing slash)
API_BASE_URL=http://localhost:3000

# ------------------------------------------------------------
# EXPO
# ------------------------------------------------------------
# Expo project slug (from app.json / expo.dev)
EXPO_PROJECT_ID=

# ============================================================
# BACKEND-ONLY VARIABLES (for /backend/.env)
# ============================================================
# These go in backend/.env, NOT in the root .env
# They are server-side secrets that must never be in the app bundle.
#
# Copy backend/.env.example → backend/.env

# PLAID SERVER SECRETS
# PLAID_CLIENT_ID=
# PLAID_SECRET=
# PLAID_ENV=sandbox

# FIREBASE ADMIN
# FIREBASE_SERVICE_ACCOUNT_KEY=<path to serviceAccountKey.json or base64 encoded>

# RAILWAY / SERVER
# PORT=3000
# NODE_ENV=development

# ENCRYPTION KEY (for storing Plaid access tokens at rest)
# ENCRYPTION_KEY=<generate with: openssl rand -hex 32>
